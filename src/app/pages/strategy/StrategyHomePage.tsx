/**
 * Strategy Home — /strategy renderiza ObjectivesDashboardPage (GS UI V2.2).
 * KPI + Tabela; UI Contract: dados exibidos via toText/renderValue.
 */
import ObjectivesDashboardPage from './ObjectivesDashboardPage'

export default function StrategyHomePage() {
  return <ObjectivesDashboardPage />
}
