/**
 * Wrapper Strategy: Approvals — reexport do Lovable kit.
 * UI Contract: dados exibidos via toText/renderValue (DataTable usa cell renderers com strings).
 */
import LovableApprovalsPage from '@/ui/lovable/pages/strategy/ApprovalsPage'

export default function ApprovalsPage() {
  return <LovableApprovalsPage />
}
