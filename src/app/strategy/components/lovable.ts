/**
 * Strategy adapter — centralizar imports do kit Lovable (contracts.md).
 * Strategy e Shell importam daqui; não espalhar paths do Lovable.
 */
export { Button, Card, Sidebar } from '@/ui/lovable'
export type { ButtonProps, CardProps, SidebarProps } from '@/ui/lovable'
