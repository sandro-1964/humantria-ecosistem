-- HUMANTRÍA — STRATEGY — GS-PATCH V1 — UI APIs (READ-ONLY)
-- Arquivo: 004_patch_seed_demo.sql
-- Status: PATCH-ONLY
--
-- Objetivo: no-op (não criar registros; patch read-only).
-- Regra: seeds só quando canonicamente necessários + evidência. Aqui é proibido criar dados.

-- no-op

