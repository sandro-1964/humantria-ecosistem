-- HUMANTRÍA — CORE — GS-PATCH V1 — UI/APIs mínimas
-- Arquivo: 001_patch_schema.sql
-- Status: PATCH (Core V1 frozen)
--
-- Objetivo: ajustes de schema (se necessário). Vazio e idempotente por padrão.

SET search_path TO core, foundation, public;

-- (sem alterações de schema neste patch)

