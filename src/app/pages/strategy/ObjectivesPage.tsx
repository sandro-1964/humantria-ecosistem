/**
 * Wrapper Strategy: Objectives — reexport do Lovable kit.
 * UI Contract: dados exibidos via toText/renderValue (DataTable usa cell renderers com strings).
 */
import LovableObjectivesPage from '@/ui/lovable/pages/strategy/ObjectivesPage'

export default function ObjectivesPage() {
  return <LovableObjectivesPage />
}
