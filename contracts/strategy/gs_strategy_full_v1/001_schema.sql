-- HUMANTRÍA — STRATEGY GS FULL V1 — Schema
-- Status: BLOQUEANTE
-- Escopo: Schema strategy (idempotente)

CREATE SCHEMA IF NOT EXISTS strategy;

COMMENT ON SCHEMA strategy IS 'Produto Strategy: objectives, initiatives, ciclos, snapshot KPIs';
